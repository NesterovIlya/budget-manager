spring:
  h2:
    console:
      enabled: true
  liquibase:
    change-log: classpath:/db/changelog/master.xml
    contexts: system-test
  jpa:
    properties:
      format_sql: true
      hibernate:
        format_sql: true
        generate_statistics: true
        dialect: org.hibernate.dialect.H2Dialect